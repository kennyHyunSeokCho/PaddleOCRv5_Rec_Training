# PaddleOCR Recognition 학습 최소 환경 설정 (.env)
# 사용법:
#   ./train.sh /Users/johyeonseog/Downloads/PaddleOCR/training_pipeline_rec/training_config.env

# ===== 저장소 (항상 자동 클론/업데이트 가능) =====
REPO_DIR=/content/PaddleOCR
REPO_GIT_URL=https://github.com/PaddlePaddle/PaddleOCR.git
AUTO_CLONE_REPO=true
# 필요 시 최신으로 동기화 (선택): true/false
REPO_GIT_UPDATE=true
# 특정 브랜치/커밋으로 고정하려면 설정 (선택)
# REPO_BRANCH=main
# REPO_REF=refs/tags/v2.7

# ===== 필수 경로 =====
BASE_CONFIG=/content/PaddleOCR/configs/rec/PP-OCRv5/multi_language/korean_PP-OCRv5_mobile_rec.yml

# 데이터셋 루트 및 라벨 파일(절대경로 권장)
DATA_DIR=/content/data/ocr_rec_dataset
TRAIN_LIST=/content/data/ocr_rec_dataset/train.txt
VAL_LIST=/content/data/ocr_rec_dataset/val.txt

# 결과 저장 디렉토리
SAVE_DIR=/content/PaddleOCR/output/rec_korean_v5

# ===== 학습 기본 =====
EPOCHS=75
BATCH_SIZE=128

# ===== GPU 설정 =====
# 단일 GPU ID 지정 (예: 0). 미설정시 CPU 사용
GPUS=0

# AMP 사용 (선택)
USE_AMP=false

# ===== 예시 데이터 자동 다운로드 (선택) =====
# PaddleOCR 공식 예시 데이터를 자동으로 다운로드할지 여부 (true/false)
DOWNLOAD_EXAMPLE_DATA=false
